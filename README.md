# `Okra test`
#### Given that your time has been limited to work on this project, make some brief notes outlining how you would proceed if you had more time (15 mins).

if I have more time;
* I will implement all the charts and make the page more interactive.
* I will implement a download report as CSV button to enable a user to download their report.
* I will add user onboarding pages (signup, login) for user authentication.
* I will add a settings menu to enable users to edit their information.


# How to Run the app locally


## Available Scripts

### `clone the project`

In the project directory, you can run:

### `yarn || npm install`
To install all the dependency
### `yarn start || npm run start`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.




